name: unit tests
on: [push]
jobs:
  unit-test:
    runs-on: ubuntu-latest
    env:
      CXX: g++-10
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: make
      - name: test
        run: ./test